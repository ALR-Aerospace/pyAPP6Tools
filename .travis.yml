language: python
python:
    - "2.7"
before_install:
    - pip install pytest pytest-cov
script:
    - ls -l
    - echo $TRAVIS_BUILD_DIR
    - cd pyAPP6Tools
    - ls -l
    - py.test tests --verbose --cov=pyAPP6Tools